display_name: "AgentOps · 层"
short_description: "层"
default_prompt: "使用 AgentOps 帮我：层"
  Run reverse-engineer in binary mode with a docs sitemap if available, generate the feature registry and specs, then validate gates.
  Keep docs say vs code proves vs hosted/control-plane separate, and do not dump embedded prompts/source into reports.

